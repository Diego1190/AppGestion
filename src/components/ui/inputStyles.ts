/**
 * Estilo base para inputs/selects/textareas de todos los formularios.
 * Antes estaba copiado y pegado (con leves diferencias) en InquilinosTab,
 * MovimientosTab, CrearCotizacionTab y GastosTab. Centralizado aquí para
 * que un cambio de estilo se aplique a todos los módulos a la vez.
 */
export const inputClass =
  'w-full px-3 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:outline-none text-sm'
